#pragma once

// Compatibility forwarding header for Vehicle FactGroups sources that include
// "FactGroup.h" and are compiled in environments where FactSystem include
// paths are not propagated consistently.
#include "../../FactSystem/FactGroup.h"
