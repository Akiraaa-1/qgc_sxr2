#pragma once

// Compatibility forwarding header for Vehicle sources that include
// "LinkInterface.h" and are compiled in environments where Comms include
// paths are not propagated consistently.
#include "../Comms/LinkInterface.h"
