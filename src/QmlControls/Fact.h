#pragma once

// Compatibility forwarding header for QmlControls sources that include
// "Fact.h" and are compiled in environments where FactSystem include paths
// are not propagated consistently.
#include "../FactSystem/Fact.h"
